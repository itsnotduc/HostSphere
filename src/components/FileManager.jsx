import React from 'react';

const FileManager = () => {
  console.log('Rendering Dashboard.jsx');
  return (
    <div>
      <p>Welcome to the FileManager page.</p>
    </div>
  );
};

export default FileManager;